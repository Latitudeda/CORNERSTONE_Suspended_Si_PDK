[build-system]
requires = []
build-backend = "flit_core.buildapi"

[project]
name = "CORNERSTONE Suspended Si pdk documentation"
authors = [{ name = "kathylin", email = "kathy_lin@enlight-tec.com" }]
dynamic = ["version", "description"]
